import { applyPipeline } from './apply'

test('can update instance content', function () {
	// the source
	const source = `
        <script>

        </sript>
    `

	// lets apply a simple transform that replaces the content with the literal expression 'hello'
	const transform = (document) => {}
})
