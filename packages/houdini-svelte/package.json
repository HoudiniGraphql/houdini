{
    "name": "houdini-svelte",
    "version": "1.4.2",
    "scripts": {
        "compile": "scripts build-go"
        "typedefs": "scripts typedefs --plugin"
    },
    "devDependencies": {
        "scripts": "workspace:^"
    }
}
