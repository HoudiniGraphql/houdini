{
	"name": "houdini-monorepo",
	"workspaces": [
		"example",
		"packages/*"
	],
	"scripts": {
		"tests": "jest",
		"build": "yarn workspace houdini-common run build && yarn workspace houdini-compiler run build && yarn workspace houdini-preprocess run build && yarn workspace houdini run build"
	},
	"devDependencies": {
		"@babel/core": "^7.12.10",
		"@babel/preset-env": "^7.12.11",
		"@types/jest": "^26.0.20",
		"babel-jest": "^26.6.3",
		"jest": "^25.0.0",
		"mock-fs": "^4.13.0",
		"prettier": "^2.0.5",
		"ts-node": "^9.1.1",
		"typescript": "^3.9.5"
	},
	"private": true,
	"dependencies": {
		"@types/mock-fs": "^4.13.0",
		"global": "^4.4.0",
		"remove": "^0.1.5"
	}
}
