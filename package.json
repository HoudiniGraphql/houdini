{
	"name": "houdini-monorepo",
	"workspaces": [
		"example",
		"packages/*"
	],
	"scripts": {
		"tests": "jest",
		"build-full": "yarn workspace houdini-compiler run build && yarn workspace houdini-preprocess run build && yarn workspace houdini run build",
		"build": "yarn workspaces foreach -pt run build"
	},
	"devDependencies": {
		"@babel/core": "^7.12.10",
		"@babel/preset-env": "^7.12.11",
		"@types/jest": "^26.0.20",
		"babel-jest": "^26.6.3",
		"jest": "^26.6.3",
		"prettier": "^2.0.5",
		"ts-node": "^9.1.1",
		"typescript": "^3.9.5"
	},
	"dependencies": {
		"houdini-compiler": "workspace:*",
		"houdini-preprocess": "workspace:*"
	},
	"private": true
}
