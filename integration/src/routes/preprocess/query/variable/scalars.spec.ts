import { expect, test } from '@playwright/test';

test.describe('query preprocessor variables', () => {
  test('query variables get marshaled from complex values', function () {
    throw new Error('not yet implemented');
  });
});
