---
'houdini': minor
'houdini-core': minor
'houdini-react': minor
'houdini-svelte': minor
---

Add the `@plural` fragment directive for spreading a fragment on a list field and reading it back as an array of data through `useFragment` (React) or `fragment` (Svelte).
