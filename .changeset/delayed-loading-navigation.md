---
'houdini': minor
'houdini-react': minor
---

Navigations now hold the previous page and only show the destination's `@loading` state when the navigation is slow, configurable via `router.loadingDelay` and `router.minDuration`.
