---
'houdini': minor
'houdini-react': minor
---

Infer typed search params for React routes: a page query's nullable variables are now exposed as an optional, type-safe `search` prop on `Link`, and required variables that aren't satisfied by a route segment are caught at build time.
